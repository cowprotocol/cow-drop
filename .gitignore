node_modules/
dist/
.turbo/
*.tsbuildinfo

# foundry
contracts/out/
contracts/cache/
# Broadcast artifacts are per-run noise; deployments/<chainId>.json is the record we keep.
# (The narrower `*/dry-run/` pattern did not match foundry's actual nesting depth.)
contracts/broadcast/

# env
.env
.env.local
*.local

# editors / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
.history/
